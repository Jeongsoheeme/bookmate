# ============================================
# Bookmate Backend - 개발 환경 오버라이드
# ============================================
# 사용법: helm install bookmate-backend . -f values.yaml -f values-dev.yaml

namespace: bookmate

backend:
  image: ghcr.io/jeongsoheeme/bookmate-backend
  tag: prod-b83136c960b6740e781ddc62dc51463e913ea13d
  replicas: 1
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi

postgres:
  password: bookmate123
  storage: 5Gi

redis:
  storage: 1Gi

secrets:
  secretKey: dev-secret-key-change-in-prod

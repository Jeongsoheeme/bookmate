apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bookmate-frontend-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Jeongsoheeme/bookmate.git
    targetRevision: main
    path: deploy/helm/frontend
    helm:
      valueFiles:
        - values.yaml
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: bookmate-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
